version: '3.8'

services:
  crawl4ai:
    image: unclecode/crawl4ai:latest
    container_name: crawl4ai
    ports:
      - "11235:11235"
    shm_size: '1g'
    environment:
      - CRAWL4AI_API_TOKEN=your_secure_api_token_here
      - CRAWL4AI_HOOKS_ENABLED=false
    restart: unless-stopped
