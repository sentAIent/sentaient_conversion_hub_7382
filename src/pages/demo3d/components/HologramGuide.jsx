import React from 'react';

const HologramGuide = ({ appId, position }) => {
  // Stubbed component to prevent build failures.
  // TODO: Replace with the actual HologramGuide implementation.
  return (
    <group position={position}>
    </group>
  );
};

export default HologramGuide;
