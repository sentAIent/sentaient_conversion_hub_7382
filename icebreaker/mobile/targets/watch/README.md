# Apple Watch Target

This directory will contain the native Swift files for the WatchOS companion app. Run `npx expo prebuild` with the appropriate config plugin once the Apple Developer certificates are configured.
